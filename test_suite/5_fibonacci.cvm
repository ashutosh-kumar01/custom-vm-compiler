show("--- 6. Testing Recursion (Fibonacci) ---\n");

fn fib(n) {
    when (n <= 1) {
        give n;
    }
    give fib(n - 1) + fib(n - 2);
}

show("Fibonacci of 10 (expected 55):\n");
show(fib(10));
